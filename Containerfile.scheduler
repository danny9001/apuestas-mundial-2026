FROM node:20-alpine
WORKDIR /app

RUN apk add --no-cache dumb-init

RUN addgroup -S appgroup && adduser -S appuser -G appgroup

COPY --chown=appuser:appgroup scheduler.js ./

USER appuser

ENTRYPOINT ["/usr/bin/dumb-init", "--"]
CMD ["node", "scheduler.js"]
